@model ManagementSystem.Models.Citizen

@{
    ViewData["Title"] = "Citizen Details";
}

<h2>Citizen Details</h2>

<div>
    <h4>Citizen Information</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Full Name</dt>
        <dd class="col-sm-10">@Model.FullName</dd>

        <dt class="col-sm-2">Address</dt>
        <dd class="col-sm-10">@Model.Address</dd>

        <dt class="col-sm-2">Phone Number</dt>
        <dd class="col-sm-10">@Model.PhoneNumber</dd>

        <dt class="col-sm-2">Email</dt>
        <dd class="col-sm-10">@Model.Email</dd>

        <dt class="col-sm-2">Date of Birth</dt>
        <dd class="col-sm-10">@(Model.DateOfBirth.ToString("yyyy-MM-dd") ?? "N/A")</dd>

        <dt class="col-sm-2">Registration Date</dt>
        <dd class="col-sm-10">@Model.RegistrationDate.ToString("yyyy-MM-dd")</dd>
    </dl>
</div>

<div>
    <a href="@Url.Action("Edit", "Citizen", new { id = Model.CitizenId })" class="btn btn-primary">Edit Citizen</a>
    <a href="/Citizen/Index" class="btn btn-secondary">Back to List</a>
</div>

<style>

/* General Styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
}

/* Page Title */
h2 {
    text-align: center;
    color: #333;
    margin-bottom: 20px;
}

a{
    text-align:center;
}

/* Citizen Info Container */
div {
    max-width: 600px;
    margin: 20px auto;
    background: white;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

/* Section Title */
h4 {
    color: #0073e6;
    text-align: center;
    margin-bottom: 10px;
}

/* Divider */
hr {
    border: 1px solid #ddd;
    margin-bottom: 20px;
}

/* Data List */
dl {
    display: flex;
    flex-wrap: wrap;
    font-size: 16px;
    color: #555;
}

dt {
    font-weight: bold;
    color: #333;
    width: 30%;
}

dd {
    width: 70%;
    margin: 0;
    padding-bottom: 10px;
}

/* Buttons */
.btn {
    display: inline-block;
    padding: 10px 15px;
    text-decoration: none;
    border-radius: 5px;
    font-size: 16px;
    margin: 10px 5px;
    text-align: center;
    font-weight: bold;
    transition: background 0.3s ease-in-out;
}

.btn-primary {
    background: #007bff;
    color: white;
}

.btn-secondary {
    background: #6c757d;
    color: white;
}

.btn-primary:hover {
    background: #0056b3;
}

.btn-secondary:hover {
    background: #545b62;
}

/* Centering Buttons */
div:last-child {
    text-align: center;
}

</style>

